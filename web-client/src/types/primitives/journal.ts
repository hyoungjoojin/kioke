export interface Journal {
  id: string;
  title: string;
  description: string;
  createdAt: string;
  lastModified: string;
}

export interface JournalPreview {
  id: string;
  title: string;
  createdAt: string;
}
