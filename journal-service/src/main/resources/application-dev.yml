spring:
  jpa:
    hibernate:
      ddl-auto: update
  datasource:
    url: jdbc:postgresql://localhost:5432/journaldb
    username: kioke
    password: password

postgres:
  user: kioke
  password: password

eureka:
  client:
    service-url:
      defaultZone: http://localhost:${SERVICE_DISCOVERY_PORT}/eureka

server:
  port: ${JOURNAL_SERVICE_PORT}
